// Thin wrapper — the Opening Explorer lives in components/ so it can also be
// embedded inline on the Learn → Openings hub (/openings).
import OpeningExplorer from "../components/OpeningExplorer";
export default function OpeningPage() { return <OpeningExplorer />; }
