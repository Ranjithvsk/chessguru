# Session log: v2 — cutover (/v2 now served by the NestJS API)

**Date:** 2026-05-29

## What
`/v2` now runs end-to-end on the new stack (React + NestJS), independent of the old Express backend.

## Steps
1. **pm2:** `chessguru-v2-api` (NestJS `dist/main.js`) started as user `ubuntu` on **:4000** with a
   real random `SESSION_SECRET` (openssl rand -hex 32) + `MONGO_URI=…/chessguru`; `pm2 save`d
   (survives reboot via the existing pm2 resurrect).
2. **nginx:** added `location /v2api/ { proxy_pass http://localhost:4000/; … }` to the chessguru
   site (backed up; `nginx -t` clean; reloaded). This is SEPARATE from the old site's `/api`, so the
   old app at `/` is untouched.
3. **web build:** `apps/web/.env.production` → `VITE_API_BASE=/v2api`, so the production bundle calls
   `/v2api/api/*` and `/v2api/auth/*` (dev still uses the Vite proxy to :3000). Rebuilt + published.

## Verified
- `/v2api/api/health|themes|puzzles/random|auth/me` all 200 through nginx.
- Browser @ /v2: network shows themes, puzzles/random, auth/me, me/rating all hitting **/v2api** →
  200; board renders a real NestJS-served puzzle (#Oy2jn). Old site `/` unaffected.
- NestJS signin verified earlier on :4000 (TestUser, bcryptjs vs existing hash).

## Notes / next
- `SESSION_SECRET` lives in the pm2 dump (not in git). Rotate/relocate if desired.
- NestJS puzzle selection is correct but simpler than the Express pool/path version (port later).
- Remaining before flipping /v2 → /: admin/Puzzle-Factory dashboards, broader parity QA.
