# 2026-09-25 — TKT-273 (coach cannot move after challenge timer) and TKT-272 (Lichess ids on leaderboard)

## TKT-273
Room `cmugwxhwnow5y`, 5 challenges. The 5th ended by timer at 6:59:25 pm IST (classEvents
`challenge_end`); the coach's class socket re-hello'd at 6:59:53 pm (six reconnects between
6:51 and 7:08 pm while LiveKit stayed up — coach's Windows PC network). The `challenge_end`
broadcast was lost; the page kept `challengeUI.active` with the countdown at 0s;
`SharedClassBoard.tsx` line ~1438 refuses coach moves while a challenge is active → "Bug not
allowing to move". Reconnect did not heal (snapshot said nothing about ended challenges) and
`challenge:end` with `room.challenge === null` returned silently.

Fix (cf36192): `room.lastChallenge`; `replayLastChallenge()` on hello (no challenge running),
on async coach seats, and on `challenge:end` with nothing running; `state.challengeActive`;
client clears a stale active challenge on a snapshot with `challengeActive === false`; the
"challenge over" notice only when the page thought one was running.

Verified with Playwright (chess-guru academy, zz room, announcement pre-seeded → no push):
1-minute challenge, context offline 52.8→74.8 s across the end; on reconnect the pill cleared
immediately and the Challenge button returned. class-ws + API restarted at 8:10 pm (no class
sockets), web deployed.

## TKT-272
`game-motifs.service.ts` `studentEvents`: labels for lichess/chesscom rows → "<student> vs
opponent" / "Opponent vs <student>"; `url` kept only for the student themself and
coach/academy_owner/admin. Leaderboard reads these via `/api/game-motifs/student/:id`.
